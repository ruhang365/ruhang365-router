# Community 内容目录

四个目录分别保存已审核的 `scenario`、`workflow`、`resource` 和 `prompt`。每个 JSON 文件既是可读内容，也是 `schemas/community-item.schema.json` 的机器输入；正式消费端只读取由 CI 生成的 Catalog。

不要在这里保存用户数据、会员内容、完整 RHZL 长文、运行结果或专项 Skill 代码。新增或修改资产时按根目录 `CONTRIBUTING.md` 执行。
