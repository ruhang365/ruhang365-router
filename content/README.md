# Community 内容目录

四个目录保留首发 14 条 `scenario`、`workflow`、`resource` 和 `prompt` 的公开审计种子。自 v0.3 起，Supabase 是唯一可编辑真相源；正式消费端优先读取 RHZL 已发布 Catalog API，本仓只分发 API 的稳定离线快照。这些文件不得作为反向数据库导入源。

不要在这里保存用户数据、会员内容、完整 RHZL 长文、运行结果或专项 Skill 代码。内容建议按根目录 `CONTRIBUTING.md` 进入审核，再由 Supabase 运营入口发布。
